# Web Design Projects

A curated set of small, self-contained websites built with **HTML/CSS** (and a bit of **JavaScript**) to demonstrate layout, UI polish, and core front-end fundamentals.

🌐 **Live demo (GitHub Pages):** https://alnvoloha.github.io/web-design/

---

## What’s inside

Each folder is a standalone project (open `index.html` inside it).

### 01) Landing Page
**A multi-section landing with navigation + mobile menu.**
- Responsive layout with clean section structure (hero, features, reviews, CTA)
- Burger/side menu interaction (JS + class toggling)
- Typography + spacing system using pure CSS

**Skills shown:** layout composition, responsive sections, simple UI interactions.

---

### 02) Resume Website
**A small multi-page resume-style site.**
- Separate pages for sections (home/works/contacts) with consistent navigation
- Structured content blocks (education, skills, portfolio sections)
- Layout built with Flexbox/Grid

**Skills shown:** multi-page structure, reusable layout patterns, content hierarchy.

---

### 03) Art Gallery
**A gallery with previews + individual artwork pages.**
- Grid-like gallery layout with links to dedicated pages
- Consistent image sizing and readable text blocks
- Clean page organization and internal navigation

**Skills shown:** content-driven layout, internal linking, typography for readability.

---

### 04) Australia Click Map
**Interactive map using native HTML image maps.**
- Clickable regions via `<map>` / `<area>`
- Separate pages per region (simple navigation by interaction)
- Legend/table to explain markers

**Skills shown:** image maps, semantic structure for “interactive content without JS”.

---

### 05) HTML Table Layout
**Data table with proper semantics and styling.**
- `caption`, `thead`, `tbody`, `tfoot` structure for readability
- Clear column alignment and spacing

**Skills shown:** semantic tables, presenting structured data, clean CSS for tabular UI.

---

### 06) Form UI (Gifts)
**A styled order form with multiple input types.**
- Inputs for name/email/quantity/date-time + select/radio groups
- `fieldset` / `legend` used to group related fields
- UI styling focused on clarity and spacing

**Skills shown:** form semantics, UI states, building clean input layouts.

---

### 07) Block Layout
**Classic “block layout” exercise focused on the box model.**
- Layout built from simple blocks with spacing and alignment
- Good for showing fundamentals: margins, padding, backgrounds, positioning

**Skills shown:** box model, basic layout mechanics, visual structure.

---

### 08) Business Card
**A compact one-page “business card” profile.**
- Minimal structure, clean typography, contact links
- Simple, readable layout that works as a micro-profile

**Skills shown:** minimal UI design, typography + spacing discipline.

---

### 09) Custom Video Player
**HTML5 video with custom controls.**
- Play/pause, seek/progress, volume, playback speed
- Time display formatting + custom UI controls
- Time-synced subtitles overlay (implemented in JS)

**Skills shown:** DOM events, HTML5 media API, building custom UI controls.

---

## How to run locally

- Open any project folder
- Run `index.html` in your browser  
  (If a browser blocks local media files, run a quick local server, e.g. VS Code “Live Server”.)

---

## Tech

**HTML • CSS • JavaScript**  

